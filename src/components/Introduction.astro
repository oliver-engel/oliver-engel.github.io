---
import Avatar from '../components/Avatar.astro';

import { Breadcrumbs } from "astro-breadcrumbs";
import 'astro-breadcrumbs/breadcrumbs.css';


// export interface Props {
// 	page: string;
//     link: string;
// }

// const { page, link } = Astro.props as Props;
---

<div class="introduction">
    <div class="flex-row">
        <!-- <Avatar />
        <p id="main-intro"></p> -->

        <Breadcrumbs indexText="Home">

            <svg
              slot="separator"
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              ><polyline points="9 18 15 12 9 6"></polyline>
            </svg>
          
          </Breadcrumbs>
    </div>

    <!-- <nav>
        <a href="/" data-page="index">Work</a>
        <a href="/words" data-page="words">Words</a>
        <a href="/about" data-page="about">About</a>
      </nav> -->

      <button data-micromodal-trigger="contact-modal">Say hey</button>


      <div class="modal micromodal-slide" id="contact-modal" aria-hidden="true">
        <div class="modal__overlay"  data-micromodal-close>
          <div class="modal__container" role="dialog" aria-modal="true" aria-labelledby="modal-1-title">
            <header class="modal__header">
                <str>Wanna chat?</str>
                <p>Open to side projects, questions, or anything else.</p>

            </header>
            <div class="modal__content" id="modal-1-content" >

                
                <button id="copy-button"></button>
                <button autofocus class="modal__close" aria-label="Close modal" data-micromodal-close>Close</button>


            </div>
      
          </div>
        </div>
      </div>

</div>

<script src="https://unpkg.com/micromodal/dist/micromodal.min.js"></script>
<link rel="stylesheet" href="https://unpkg.com/microtip/microtip.css">

<script>

    document.addEventListener("DOMContentLoaded", function() {

        try {

        MicroModal.init({
            awaitCloseAnimation: true,// set to false, to remove close animation
            onShow: function(modal) {
            console.log("micromodal open");
            },
            onClose: function(modal) {
            console.log("micromodal close");
            }
        });

        } catch (e) {
        console.log("micromodal error: ", e);
        }

    });
    // Get the current page's filename (e.g., "index.html", "about.html", etc.)
    const currentPage = window.location.pathname.split('/').pop();

    // Get references to the navigation links
    const navLinks = document.querySelectorAll('nav a');

    // Loop through the navigation links and update the active link
    navLinks.forEach(link => {
    const linkPage = link.getAttribute('data-page');
    
    // Handle the homepage case
    if (currentPage === "") {
        if (linkPage === "index" || link.href.endsWith("/")) {
        link.classList.add('active');
        }
    } else if (linkPage === currentPage) {
        link.classList.add('active');
    }
    });

    

</script>



<style>

    #copy-button{
        transition: 0.2s all;
    }

    #copy-button:before{
        content:"oliverengel6@gmail.com";
    }

    #copy-button:hover:before{
        content:"Click to copy";
    }

    #main-intro:before{
        content: "Hello Internet user, I’m Oliver – a software designer.";
    }

    @media (max-width: 700px) {
        #main-intro:before {
            content: "Oliver Engel";
        }
    }

    nav a.active {
        color:var(--accent);
        border-bottom: 2px solid var(--accent);
    }

    nav{
        display: flex;
        gap: 16px;
    }

    nav a {
        transition: 0.2s all;
        border-bottom:2px solid var(--background);
        /* opacity: .8; */
    }

    nav a:hover{
        opacity: 1;
        /* border-bottom:2px solid var(--background-secondary); */

    }

    .introduction{
        /* position: fixed; */
        /* text-align: left; */
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        z-index:1000;
        /* background: #efefec; */
        max-width: 1100px;
        width: 100%;
        margin: 24px auto;
        /* top:12px;
        left:12px;
        right:12px; */
        /* padding: 16px 32px 12px 32px; */
        border-radius: 1000px;
    }

    p{
        margin: 0;
    }





</style>